<?php

require_once "vendor/autoload.php";

$inputFilename = isset($argv[1]) ? $argv[1] : 'import.csv';
$outputFilename = isset($argv[2]) ? $argv[2] : 'import.xml';

$converter = new CSV2XML($inputFilename, $outputFilename);

$converter->parseCSV();

$converter->export();